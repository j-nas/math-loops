import {useState} from 'react';
import {Operator} from './types';

function App() {
  const [opA, setOpA] = useState(0);
  const [opB, setOpB] = useState(0);
  const [operator, setOperator] = useState('');

  const pickNumbers = () => {};
  return <div className="App">this is a div</div>;
}

export default App;
